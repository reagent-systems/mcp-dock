import { describe, expect, test } from 'vitest'

describe('e2e', () => {
  test.skip('Playwright e2e removed for MCP Dock scaffold; run the app with npm run dev', () => {
    expect(true).toBe(true)
  })
})
